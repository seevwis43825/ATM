# AI Banking Agent

> 人工智能赛道参赛作品：用自然语言对话完成银行业务的 Agent 系统。

---

## 一句话介绍

用户打开网页，像聊天一样说"给张三转 500 块，备注房租"，Agent 理解意图、自主规划、调用银行工具、生成确认卡片，用户点头即完成。账单分析、理财申购、卡片管理同理。

---

## 团队与分工

| 代号 | 角色 | 负责模块 | 主要 AI 工具 |
|---|---|---|---|
| **A** | Agent 架构 / 技术负责人 | 大脑层、意图识别、工具编排、Mock 数据、跨场景联动 | WorkBuddy |
| **B** | 转账场景 + 部署负责人 | 智能转账、定时转账、拆分 AA、部署上线、仓库维护 | Codex |
| **C** | 账单分析负责人 | 消费分类统计、异常交易识别、账单报告、演示视频 | WorkBuddy |
| **D** | 理财 + 卡片负责人 | 产品推荐对比、风险评估、申购赎回、卡片全流程 | Cursor |
| **E** | 前端 + 答辩负责人 | Web 界面、聊天窗、图表渲染、答辩 PPT、演讲稿 | Cursor |

> 每个人都是"场景 Owner"：从工具函数、到测试用例、到 PPT 里那两页、到视频里那 30 秒，全包。

---

## 目录结构

```
AI-Banking/
├── banking-core/
│   ├── contracts/          # 接口契约（全队宪法，改这里要举手）
│   └── mock-data/          # 统一 Mock 数据（A 维护，其他人只读）
├── apps/
│   ├── api/                # 后端：Agent + 业务工具
│   │   └── src/
│   │       ├── agent/      # 大脑：意图理解、规划、编排
│   │       └── tools/      # 业务工具：按场景分文件
│   └── web/                # 前端：移动端壳子 + 聊天窗
│       └── src/
│           ├── pages/      # 页面
│           └── components/ # 组件
├── docs/
│   ├── 01-tech/            # 【交付物①】技术文档
│   ├── 02-ppt/             # 【交付物②】答辩 PPT
│   ├── 03-deploy/          # 【交付物③】README/部署/测试用例
│   ├── 04-security/        # 【交付物④】安全自评报告
│   └── 05-video/           # 【交付物⑤】演示视频脚本
└── scripts/                # 一键启动等辅助脚本
```

---

## 快速开始（每个组员 clone 后照做）

```bash
git clone <仓库地址>
cd AI-Banking

# 1. 后端
cd apps/api
cp .env.example .env        # 填入自己的 key（没有也能跑 Mock 模式）
npm install
npm run dev                 # 默认 3001 端口

# 2. 前端（另开一个终端）
cd apps/web
npm install
npm run dev                 # 默认 5173 端口
```

浏览器打开 `http://localhost:5173`，看到聊天界面即成功。

> **没配 AI key 也能跑**：项目默认 `MOCK_MODE=true`，所有工具返回预置数据。这样任何人在任何时间 clone 下来都能立即看到完整流程，不用等别人。

---

## 交付物对照表

| # | 交付物 | 硬性要求 | 负责人 | 存放位置 |
|---|---|---|---|---|
| 01 | 技术文档 | PDF/DOCX ≤50MB，含架构图、核心算法说明、安全设计 | A | `docs/01-tech/` |
| 02 | 答辩材料 | PPT/PPTX/PDF ≤80MB，汇报 ≤10 分钟 | E | `docs/02-ppt/` |
| 03 | 源代码与部署材料 | ZIP ≤300MB，含 README、部署说明、测试用例；附仓库链接 | B | `docs/03-deploy/` |
| 04 | 安全自评报告 | PDF/DOCX ≤50MB，权限分级说明 + 已知风险清单 | D | `docs/04-security/` |
| 05 | 演示视频 | MP4 ≤500MB，≤5 分钟 | C | `docs/05-video/` |

---

## 协作铁律（违反 = 全队返工）

1. **不直接推 main。** 所有改动走 `feature/xxx` 分支 + Pull Request。
2. **契约优先。** 要改 `banking-core/contracts/` 里的任何东西，先在群里说，改完更新版本号。
3. **Mock 先行。** 每个人第一天就把自己场景的假数据跑通，不依赖别人。
4. **每天 commit。** 哪怕只改了注释。项目经理（A）每天睡前看一次贡献图。
5. **接口不等人。** 如果 B 的工具没写完，A 用 Mock 版占位，绝不阻塞集成。
6. **文档跟代码同分支提交。** 不要最后一周才补文档——那是灾难。

---

## 里程碑

| 阶段 | 时间 | 目标 |
|---|---|---|
| 第 1 周 | 9.12 – 9.18 | 契约冻结、骨架跑通、Mock 数据就位 |
| 第 2-3 周 | 9.19 – 10.2 | 3 个必选场景打通（转账 / 账单 / 理财） |
| 第 4-5 周 | 10.3 – 10.16 | 全场景覆盖 + 前端成型 + 部署上线 |
| 第 6 周 | 10.17 – 10.23 | 文档、安全报告、PPT 初稿 |
| 第 7 周 | 10.24 – 10.30 | 演示视频、答辩彩排、Bug 修复 |
| 第 8 周 | 10.31 – 11.5 | 冻结、打包、提交流程演练 |
| **提交日** | **11.6** | **全部交付物上传** |
